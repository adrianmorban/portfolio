import React from 'react';

function HeaderText(props){
    return (
        <div className="headerText">
            <h1>{props.Texto}</h1>
        </div>
           );
}

export default HeaderText;
